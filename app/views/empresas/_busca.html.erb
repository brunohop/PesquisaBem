<%= simple_form_for((@empresa), :url => "buscar", :method => :get ) do |f|%>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :razaoSocial, label:'Razão Social' %>
    <%= f.input :cnpj, label:'CNPJ' %>
  </div>

<div class="form-actions">
  <%= f.button :submit, 'Buscar' %>
</div>

<% end %>
